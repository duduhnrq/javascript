function soma(n1=0, n2=0) { // o "=0" depois dos parametros significa que se não houver valor definido ele vai considerar 0
    return n1 + n2
}

console.log(soma(2)) // Nesse caso só defini o valor do n1, então n2 será 0